# Frontend configuration (Create React App)
# Ask the orchestrator to add these to the container's .env as needed.

# Base URL for the Express REST API (no trailing slash).
# - local dev: http://localhost:3001
# - preview:   https://<backend-preview-host>
REACT_APP_API_BASE_URL=http://localhost:3001

# Full WebSocket URL.
# If omitted, the app derives it from REACT_APP_API_BASE_URL (http->ws, https->wss) and uses /ws.
# - local dev: ws://localhost:3001/ws
# - preview:   wss://<backend-preview-host>/ws
REACT_APP_WS_URL=ws://localhost:3001/ws
